A) #Indexing is the calculating the position of an item in a list whether it be a number or word

B) # its useful so you can obtain an element from a list and manipulate it

C) #Indexing just finds one element and can only change one thing. Dictionary elements can have values that you can change too
